name: blank

on:
  push:

jobs:
  continent:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        defect: ["detail", "admission", "dirty", "fail", "pierce"]
        vice: ["curiosity", "provide", "therapy", "radiation"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar CA
